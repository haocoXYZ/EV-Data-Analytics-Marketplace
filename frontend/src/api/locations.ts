import client from "./client"; import { Province, District } from "../types"; export const locationsApi = { getProvinces: async (): Promise<Province[]> => { const response = await client.get<Province[]>("/locations/provinces"); return response.data; }, getDistricts: async (provinceId?: number): Promise<District[]> => { const response = await client.get<District[]>("/locations/districts", { params: provinceId ? { provinceId } : undefined }); return response.data; }, };
