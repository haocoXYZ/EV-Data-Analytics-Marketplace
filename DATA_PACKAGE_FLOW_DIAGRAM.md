# 🔄 Data Package Flow - Visual Diagrams

## 📊 Complete System Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    EV DATA MARKETPLACE                              │
│                  Data Package Flow Diagram                          │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────┐         ┌──────────────┐         ┌─────────────┐
│  Provider   │         │   Platform   │         │  Consumer   │
│ (VinFast)   │         │  (Database)  │         │ (Customer)  │
└──────┬──────┘         └──────┬───────┘         └──────┬──────┘
       │                       │                        │
       │ 1. Upload Dataset     │                        │
       │──────────────────────>│                        │
       │   (904 records)       │                        │
       │                       │                        │
       │                       │ 2. Browse Marketplace  │
       │                       │<───────────────────────│
       │                       │    (Select location)   │
       │                       │                        │
       │                       │ 3. Preview Package     │
       │                       │<───────────────────────│
       │                       │   (See price & rows)   │
       │                       │                        │
       │                       │ 4. Purchase Package    │
       │                       │<───────────────────────│
       │                       │   POST /api/datapackage│
       │                       │         /purchase      │
       │                       │                        │
       │                ┌──────▼──────┐                 │
       │                │   Payment   │                 │
       │                │   Gateway   │                 │
       │                │   (PayOS)   │                 │
       │                └──────┬──────┘                 │
       │                       │                        │
       │                       │ 5. Payment Webhook     │
       │                       │<───────────────────────│
       │                       │   (Status → Active)    │
       │                       │                        │
       │                       │ 6. View Purchases      │
       │                       │<───────────────────────│
       │                       │   GET /api/purchases   │
       │                       │       /my-data-packages│
       │                       │                        │
       │                       │ 7. Download CSV        │
       │                       │<───────────────────────│
       │                       │   GET /api/purchases   │
       │                       │       /download/{id}   │
       │                       │                        │
       │                       │ 8. Receive CSV File    │
       │                       │───────────────────────>│
       │                       │   (400 rows of data)   │
       │                       │                        │
       │ 9. Calculate Revenue  │                        │
       │<──────────────────────│                        │
       │   (70% = 2,800 VND)   │                        │
       │                       │                        │
       │ 10. Receive Payout    │                        │
       │<──────────────────────│                        │
       │                       │                        │
```

---

## 🛒 Purchase Flow (Detailed)

```
┌──────────────────────────────────────────────────────────────────┐
│                      PURCHASE FLOW                               │
└──────────────────────────────────────────────────────────────────┘

Customer Journey:
─────────────────

1. SELECT LOCATION
   ┌─────────────────────┐
   │  Data Packages      │
   │  ┌───────────────┐  │
   │  │ Province: ▼   │  │  ← Select "Hà Nội"
   │  └───────────────┘  │
   │  ┌───────────────┐  │
   │  │ District: ▼   │  │  ← Select "All" or specific
   │  └───────────────┘  │
   └─────────────────────┘
           ↓

2. SELECT DATE RANGE (Optional)
   ┌─────────────────────┐
   │  Date Range         │
   │  ┌───────────────┐  │
   │  │ From: [Date]  │  │  ← 2024-01-01
   │  └───────────────┘  │
   │  ┌───────────────┐  │
   │  │ To:   [Date]  │  │  ← 2024-03-31
   │  └───────────────┘  │
   └─────────────────────┘
           ↓

3. PREVIEW & CALCULATE
   ┌─────────────────────────────┐
   │  Package Preview            │
   │                             │
   │  Location: Hà Nội           │
   │  Date Range: Q1 2024        │
   │  Available Rows: 400        │
   │                             │
   │  Price per Row: 10 VND      │
   │  ─────────────────────      │
   │  Total Price: 4,000 VND     │
   │                             │
   │  [Cancel]  [Purchase]       │
   └─────────────────────────────┘
           ↓

4. CONFIRM PURCHASE
   POST /api/datapackage/purchase
   {
     "provinceId": 1,
     "districtId": null,
     "startDate": "2024-01-01",
     "endDate": "2024-03-31",
     "rowCount": 400,
     "pricePerRow": 10.0,
     "totalPrice": 4000.0
   }
           ↓

5. DATABASE RECORD CREATED
   DataPackagePurchase:
   ┌─────────────────────────────┐
   │ PurchaseId: 1               │
   │ ConsumerId: 1               │
   │ ProvinceId: 1               │
   │ DistrictId: NULL            │
   │ StartDate: 2024-01-01       │
   │ EndDate: 2024-03-31         │
   │ RowCount: 400               │
   │ PricePerRow: 10.00          │
   │ TotalPrice: 4000.00         │
   │ Status: Pending             │ ← Awaiting payment
   │ PurchaseDate: 2025-11-03    │
   │ DownloadCount: 0            │
   │ MaxDownload: 5              │
   │ LastDownloadDate: NULL      │
   └─────────────────────────────┘
           ↓

6. PAYMENT GATEWAY
   ┌─────────────────────────────┐
   │      PayOS Payment          │
   │                             │
   │  Amount: 4,000 VND          │
   │  Order ID: 1762148031       │
   │                             │
   │  [QR Code]                  │
   │                             │
   │  Scan to pay with:          │
   │  - Banking app              │
   │  - E-wallet                 │
   └─────────────────────────────┘
           ↓

7. PAYMENT COMPLETED
   PayOS Webhook → /api/webhook/payos
   {
     "orderCode": 1762148031,
     "status": "PAID"
   }
           ↓

8. STATUS UPDATED
   DataPackagePurchase:
   ┌─────────────────────────────┐
   │ Status: Pending → Active ✅ │
   └─────────────────────────────┘
           ↓

9. READY TO DOWNLOAD
   ┌─────────────────────────────┐
   │  ✅ Purchase Complete!      │
   │                             │
   │  You can now download       │
   │  your data package.         │
   │                             │
   │  [Go to My Purchases]       │
   └─────────────────────────────┘
```

---

## 📥 Download Flow (Detailed)

```
┌──────────────────────────────────────────────────────────────────┐
│                      DOWNLOAD FLOW                               │
└──────────────────────────────────────────────────────────────────┘

Customer Actions:
─────────────────

1. NAVIGATE TO MY PURCHASES
   URL: /my-purchases
   ┌─────────────────────────────────────────────┐
   │  My Purchases                               │
   │  ┌────────┬──────────┬────────┐            │
   │  │ Data   │ Subscr.  │ API    │            │
   │  │Packages│          │Packages│            │
   │  └────────┴──────────┴────────┘            │
   └─────────────────────────────────────────────┘
           ↓

2. VIEW DATA PACKAGES
   GET /api/purchases/my-data-packages
   ┌───────────────────────────────────────────────────────┐
   │ Data Packages                                         │
   ├───────┬───────────┬────┬───────┬────────┬─────────┬──┤
   │ Date  │ Location  │Rows│ Price │ Status │Downloads│  │
   ├───────┼───────────┼────┼───────┼────────┼─────────┼──┤
   │11/03  │Hà Nội     │400 │4,000  │Active  │ 1/5     │[📥]│ ← Can download
   │       │All        │    │       │        │         │  │
   ├───────┼───────────┼────┼───────┼────────┼─────────┼──┤
   │11/03  │Hà Nội     │402 │4,020  │Active  │ 0/5     │[📥]│ ← Fresh download
   │       │All        │    │       │        │         │  │
   ├───────┼───────────┼────┼───────┼────────┼─────────┼──┤
   │11/03  │Hà Nội     │402 │4,020  │Pending │ 0/5     │[🚫]│ ← Not paid yet
   │       │All        │    │       │        │         │  │
   └───────┴───────────┴────┴───────┴────────┴─────────┴──┘
           ↓

3. CLICK DOWNLOAD BUTTON
   Customer clicks [📥] on Purchase #2
           ↓

4. FRONTEND VALIDATION
   if (status !== 'Active') {
     ❌ Disable button
     return;
   }
   if (downloadCount >= maxDownload) {
     ❌ Disable button
     return;
   }
   ✅ Proceed to download
           ↓

5. API REQUEST
   GET /api/purchases/download/2
   Headers: {
     Authorization: "Bearer eyJhbGciOiJIUzI1..."
   }
           ↓

6. BACKEND VALIDATION
   ┌─────────────────────────────────────┐
   │ PurchasesController                 │
   │                                     │
   │ ✓ Verify JWT token                 │
   │ ✓ Get user from token              │
   │ ✓ Get consumer profile             │
   │ ✓ Find purchase by ID              │
   │ ✓ Verify ownership                 │
   │ ✓ Check status = "Active"          │
   │ ✓ Check downloadCount < maxDownload│
   │                                     │
   │ All checks passed ✅                │
   └─────────────────────────────────────┘
           ↓

7. GENERATE CSV DATA
   ┌─────────────────────────────────────────┐
   │ GenerateMockCSVData(purchase)           │
   │                                         │
   │ Input:                                  │
   │   - Province: Hà Nội                    │
   │   - Row Count: 402                      │
   │   - Date Range: NULL (all dates)        │
   │                                         │
   │ Process:                                │
   │   1. Create CSV header                  │
   │   2. Loop 402 times                     │
   │   3. Generate random but realistic data:│
   │      - Station names                    │
   │      - Charging timestamps              │
   │      - Energy consumption               │
   │      - Costs                            │
   │      - Vehicle models                   │
   │      - Battery stats                    │
   │                                         │
   │ Output: CSV string (402 rows)           │
   └─────────────────────────────────────────┘
           ↓

8. UPDATE DATABASE
   DataPackagePurchase (ID=2):
   ┌─────────────────────────────┐
   │ DownloadCount: 0 → 1        │
   │ LastDownloadDate: NOW()     │
   └─────────────────────────────┘
           ↓

9. RETURN CSV FILE
   HTTP Response:
   ┌─────────────────────────────────────────────┐
   │ Status: 200 OK                              │
   │ Content-Type: text/csv                      │
   │ Content-Disposition: attachment;            │
   │   filename="ev_charging_data_HàNội_        │
   │            20250104_143022.csv"             │
   │                                             │
   │ Body: CSV data (402 rows)                   │
   └─────────────────────────────────────────────┘
           ↓

10. BROWSER DOWNLOADS FILE
   ┌─────────────────────────────┐
   │  Chrome Downloads           │
   │                             │
   │  📄 ev_charging_data_       │
   │     HàNội_20250104.csv      │
   │     [Open] [Show in folder] │
   └─────────────────────────────┘
           ↓

11. UPDATE UI
   ┌────────────────────────────────────────┐
   │ ✅ Data package downloaded successfully!│
   └────────────────────────────────────────┘
   
   Downloads counter: 0/5 → 1/5
           ↓

12. CUSTOMER USES DATA
   ┌─────────────────────────────┐
   │  Excel / Python / R         │
   │                             │
   │  - Analyze charging patterns│
   │  - Create reports           │
   │  - Build dashboards         │
   │  - Train ML models          │
   └─────────────────────────────┘
```

---

## 🔐 Security & Validation Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                   SECURITY VALIDATION FLOW                       │
└──────────────────────────────────────────────────────────────────┘

Download Request:
─────────────────

GET /api/purchases/download/2
Headers: { Authorization: "Bearer {token}" }
        ↓

╔═══════════════════════════════════════╗
║  VALIDATION CHECKPOINT 1: Auth       ║
╚═══════════════════════════════════════╝
        ↓
Has JWT token?
├─ No  → ❌ 401 Unauthorized
└─ Yes → Continue
        ↓
Token valid?
├─ No  → ❌ 401 Unauthorized (Invalid token)
└─ Yes → Continue
        ↓
Extract email from token
        ↓

╔═══════════════════════════════════════╗
║  VALIDATION CHECKPOINT 2: User       ║
╚═══════════════════════════════════════╝
        ↓
User exists in database?
├─ No  → ❌ 401 Unauthorized
└─ Yes → Continue
        ↓
User role = "DataConsumer"?
├─ No  → ❌ 403 Forbidden
└─ Yes → Continue
        ↓
Get ConsumerId from User
        ↓

╔═══════════════════════════════════════╗
║  VALIDATION CHECKPOINT 3: Purchase   ║
╚═══════════════════════════════════════╝
        ↓
Purchase exists (ID=2)?
├─ No  → ❌ 404 Not Found
└─ Yes → Continue
        ↓
Purchase.ConsumerId == Current User?
├─ No  → ❌ 404 Not Found (Hide existence)
└─ Yes → Continue
        ↓

╔═══════════════════════════════════════╗
║  VALIDATION CHECKPOINT 4: Status     ║
╚═══════════════════════════════════════╝
        ↓
Purchase.Status == "Active"?
├─ No  → ❌ 400 Bad Request
│        "This purchase is not active"
└─ Yes → Continue
        ↓

╔═══════════════════════════════════════╗
║  VALIDATION CHECKPOINT 5: Limit      ║
╚═══════════════════════════════════════╝
        ↓
DownloadCount < MaxDownload?
├─ No  → ❌ 400 Bad Request
│        "Download limit reached (5/5)"
└─ Yes → Continue
        ↓

╔═══════════════════════════════════════╗
║  ✅ ALL CHECKS PASSED                ║
║  Proceed to generate & return CSV    ║
╚═══════════════════════════════════════╝
```

---

## 💰 Revenue Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                      REVENUE SHARING FLOW                        │
└──────────────────────────────────────────────────────────────────┘

Customer Purchase:
──────────────────

Customer pays: 4,000 VND
        ↓
╔═══════════════════════════════════════════════════════════╗
║                     REVENUE SPLIT                         ║
║                                                           ║
║  Total: 4,000 VND                                         ║
║  ├─ Provider Share (70%): 2,800 VND                       ║
║  └─ Platform Share (30%): 1,200 VND                       ║
╚═══════════════════════════════════════════════════════════╝
        ↓
┌───────────────────┐         ┌────────────────────┐
│  RevenueShare     │         │  Payout            │
│  ─────────────    │         │  ───────           │
│  PaymentId: 5     │         │  ProviderId: 1     │
│  ProviderId: 1    │         │  Month: 2025-11    │
│  TotalAmount:     │         │  TotalDue:         │
│    4,000          │         │    5,614 VND       │
│  ProviderShare:   │   ────> │  Status: Pending   │
│    2,800          │         │                    │
│  AdminShare:      │         │  (Accumulated from │
│    1,200          │         │   multiple sales)  │
│  Status: Pending  │         │                    │
└───────────────────┘         └────────────────────┘
        ↓                              ↓
Provider receives payout         Platform keeps fee
at end of month                  for operations
```

---

## 📊 Database Entity Relationships

```
┌──────────────────────────────────────────────────────────────────┐
│                    DATABASE SCHEMA                               │
└──────────────────────────────────────────────────────────────────┘

┌─────────────┐
│    User     │
│─────────────│
│ UserId (PK) │
│ Email       │
│ Password    │
│ Role        │────────┐
└─────────────┘        │
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ↓                             ↓
┌─────────────────┐         ┌─────────────────┐
│ DataProvider    │         │ DataConsumer    │
│─────────────────│         │─────────────────│
│ ProviderId (PK) │         │ ConsumerId (PK) │
│ UserId (FK)     │         │ UserId (FK)     │
│ CompanyName     │         │ Organization    │
└────────┬────────┘         └────────┬────────┘
         │                           │
         │                           │
         │                           ↓
         │                  ┌─────────────────────────┐
         │                  │ DataPackagePurchase     │
         │                  │─────────────────────────│
         │                  │ PurchaseId (PK)         │
         │                  │ ConsumerId (FK) ────────┘
         │                  │ ProvinceId (FK)
         │                  │ DistrictId (FK)
         │                  │ StartDate
         │                  │ EndDate
         │                  │ RowCount
         │                  │ PricePerRow
         │                  │ TotalPrice
         │                  │ Status
         │                  │ DownloadCount  ← Tracks downloads
         │                  │ MaxDownload    ← Limit (5)
         │                  │ LastDownloadDate
         │                  └────────┬────────┘
         │                           │
         │                           ↓
         │                  ┌─────────────────┐
         │                  │    Payment      │
         │                  │─────────────────│
         │                  │ PaymentId (PK)  │
         │                  │ ConsumerId (FK) │
         │                  │ Amount          │
         │                  │ Status          │
         │                  │ ReferenceId ────┘
         │                  └────────┬────────┘
         │                           │
         │                           ↓
         │                  ┌─────────────────────┐
         │                  │  RevenueShare       │
         │                  │─────────────────────│
         │                  │ ShareId (PK)        │
         │                  │ PaymentId (FK) ─────┘
         │                  │ ProviderId (FK) ────┐
         └──────────────────┼─────────────────────┘
                            │ TotalAmount
                            │ ProviderShare
                            │ AdminShare
                            │ PayoutStatus
                            └─────────────────────┘

┌─────────────┐
│  Province   │
│─────────────│
│ ProvinceId  │───┐
│ Name        │   │
│ Code        │   │
└─────────────┘   │
                  ├─── Used for location filtering
┌─────────────┐   │
│  District   │   │
│─────────────│   │
│ DistrictId  │───┘
│ ProvinceId  │
│ Name        │
└─────────────┘

┌─────────────────┐
│  Dataset        │
│─────────────────│
│ DatasetId (PK)  │
│ ProviderId (FK) │───┐
│ Name            │   │
│ RowCount        │   │
│ Status          │   │
│ ModerationStatus│   │
└─────────────────┘   │
                      │
                      ↓
          ┌──────────────────────┐
          │  DatasetRecords      │
          │──────────────────────│
          │ RecordId (PK)        │
          │ DatasetId (FK) ──────┘
          │ ProvinceId (FK)
          │ DistrictId (FK)
          │ StationId
          │ StationName
          │ ChargingTimestamp
          │ EnergyKwh
          │ PowerKw
          │ VehicleType
          │ ... (17 more fields)
          └──────────────────────┘
                ↑
                │
                └─── Source data for CSV download
                     (904 records currently)
```

---

## 🔄 State Machine: Purchase Status

```
┌──────────────────────────────────────────────────────────────────┐
│                  PURCHASE STATUS STATE MACHINE                   │
└──────────────────────────────────────────────────────────────────┘

                    ┌─────────────┐
                    │   Pending   │ ← Initial state after purchase
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        │ Payment Success  │ Payment Failed   │ Timeout/Cancel
        │ (PayOS webhook)  │                  │
        ↓                  ↓                  ↓
  ┌──────────┐       ┌──────────┐      ┌──────────┐
  │  Active  │       │  Failed  │      │Cancelled │
  └────┬─────┘       └──────────┘      └──────────┘
       │                   ↑                  ↑
       │                   │                  │
       │                   └──────────────────┘
       │                   (Can retry payment)
       │
       │ Download CSV (up to 5 times)
       │ DownloadCount: 0 → 1 → 2 → 3 → 4 → 5
       │
       │ (Status remains Active)
       │
       ↓
  ┌──────────────────────┐
  │   Active (5/5)       │ ← Max downloads reached
  │   Cannot download    │    (but still Active)
  └──────────────────────┘


Status Rules:
─────────────
Pending  → Can view, cannot download
Active   → Can view, can download (if downloadCount < maxDownload)
Failed   → Can view, cannot download
Cancelled→ Can view, cannot download

Download Rules:
───────────────
downloadCount < maxDownload → Button enabled ✅
downloadCount >= maxDownload → Button disabled ❌
status != "Active" → Button disabled ❌
```

---

## 🌐 API Endpoint Map

```
┌──────────────────────────────────────────────────────────────────┐
│                      API ENDPOINTS MAP                           │
└──────────────────────────────────────────────────────────────────┘

Base URL: http://localhost:5292/api

Authentication:
───────────────
POST   /auth/login
       → Get JWT token
       Input: { email, password }
       Output: { token }

Data Packages:
──────────────
GET    /datapackage/preview
       → Preview package (rows & price)
       Params: ?provinceId=1&districtId=1&startDate=...&endDate=...
       Output: { rowCount, pricePerRow, totalPrice }

POST   /datapackage/purchase
       → Create purchase order
       Input: { provinceId, districtId, startDate, endDate, ... }
       Output: { purchaseId, paymentUrl }

Purchases:
──────────
GET    /purchases/my-purchases
       → Get all purchases (data packages, subscriptions, API packages)
       Output: { dataPackages[], subscriptions[], apiPackages[] }

GET    /purchases/my-data-packages
       → Get only data package purchases
       Output: [{ purchaseId, provinceName, rowCount, ... }]

GET    /purchases/download/{purchaseId}  ⭐ MAIN DOWNLOAD ENDPOINT
       → Download CSV for purchased package
       Output: CSV file

Payment:
────────
POST   /webhook/payos
       → Receive payment confirmation from PayOS
       Input: { orderCode, status }
       Action: Update purchase status to Active

Alternative (Old):
──────────────────
GET    /datapackage/{purchaseId}/download
       → Alternative download endpoint (uses real DatasetRecords)
       Output: CSV file
```

---

**Visual Reference Complete! 🎉**

Use these diagrams to understand:
1. 🔄 Complete system flow
2. 🛒 Purchase process
3. 📥 Download mechanism
4. 🔐 Security validations
5. 💰 Revenue distribution
6. 📊 Database relationships
7. 🔄 Status state machine
8. 🌐 API endpoints

